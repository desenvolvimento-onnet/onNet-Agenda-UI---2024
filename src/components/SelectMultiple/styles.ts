import styled from "styled-components";

export const Form = styled.form`
  padding: 0 1rem;

  p {
    padding: 0.5rem 0;

    font-size: 11pt;
    text-align: center;
  }

  hr {
    margin-bottom: 0.5rem;
  }

  > * {
    width: 100%;
  }
`;
